package menu

import org.junit.Test

import org.junit.Assert.*

class RendererTest {

    @Test
    fun dailyRenderer() {
//        val renderer = Renderer.buildRender(Renderer.Type.DAILY)
//        assertNotNull(renderer)
//        assertEquals("Daily", renderer.render())
    }

    @Test
    fun weeklyRenderer() {
//        val renderer = Renderer.buildRender(Renderer.Type.WEEKLY)
//        assertNotNull(renderer)
//        assertEquals("Weekly", renderer.render())
    }
}